# ==============================================
# 依赖与包管理相关（核心必忽略）
# ==============================================
node_modules/          # 所有依赖安装目录
.pnpm/                 # pnpm 缓存目录
.pnpm-workspace-state-v1.json  # pnpm 工作区状态文件
.modules.yaml          # pnpm 模块映射文件
pnpm-debug.log         # pnpm 调试日志
npm-debug.log*         # npm 日志（防止误操作使用npm）
yarn.lock              # 若不用yarn，忽略其锁文件
package-lock.json      # 若不用npm，忽略其锁文件

# ==============================================
# 构建与打包产物（根据你的项目实际输出补充）
# ==============================================
dist/                  # Vite 默认构建输出目录
out/                   # Electron 打包输出（如electron-builder）
build/                 # 通用构建目录
release/               # 发布包目录（若有）
*.exe                  # 生成的可执行文件（如Windows）
*.dmg                  # Mac 安装包
*.deb                  # Linux 安装包
*.snap                 # Linux 快照包
electron/dist/         # 若Electron单独构建，指定其目录

# ==============================================
# 环境变量与敏感信息
# ==============================================
.env                   # 环境变量（包含密钥等）
.env.local             # 本地环境变量
.env.*.local           # 特定环境的本地变量
!.env.example          # 保留示例配置（若有）
!.env.template         # 保留模板文件（若有）

# ==============================================
# 日志、缓存与临时文件
# ==============================================
logs/                  # 日志目录
tmp/                   # 临时文件目录
*.log                  # 所有日志文件
*.swp                  # Vim 临时文件
*.swo                  # Vim 临时文件
.DS_Store              # Mac 系统文件
Thumbs.db              # Windows 系统文件
.cache/                # 缓存目录（如工具缓存）
.eslintcache           # ESLint 缓存

# ==============================================
# IDE 配置（避免上传个人配置）
# ==============================================
.vscode/               # VS Code 配置（包含个人设置、插件缓存等）
!.vscode/extensions.json  # 保留团队共享的推荐插件列表（可选）
!.vscode/settings.json    # 保留团队共享的项目设置（可选）
!.vscode/tasks.json       # 保留团队共享的任务配置（可选）
.idea/                 # WebStorm 等JetBrains IDE配置
*.suo                  # Visual Studio 配置
*.sdf                  # Visual Studio 数据库文件
*.opensdf              # Visual Studio 临时文件

# ==============================================
# 测试与覆盖率相关
# ==============================================
coverage/              # 测试覆盖率报告
*.spec.js              # 测试文件（若不想上传测试代码，否则删除此行）
*.test.js              # 测试文件
*.jest.js              # Jest 测试文件
.nyc_output/           # nyc 覆盖率输出

# ==============================================
# 项目特定临时文件（根据你的业务补充）
# ==============================================
downloads/             # 若有本地下载缓存目录，忽略
videos/                # 若有临时视频文件目录，忽略
*.tmp                  # 自定义临时文件后缀
node_modules
